import './App.css';
import FilterableWeaponTable from './FilterableWeaponTable';


function App() {
    return (
        <div className="App">
            add styling, add input validation, fix sorting for scaling letters, add google tracking
            <FilterableWeaponTable />

        </div>
    );
}

export default App;
