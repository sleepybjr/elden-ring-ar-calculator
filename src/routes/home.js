import UserBuild from "../userBuild/UserBuild";

export default function Home() {

    return (
        <div className='extra-spacing'>
            WIP.
            {/* <UserBuild/> */}
        </div>
    );
}